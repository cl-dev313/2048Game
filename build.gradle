// 项目根目录的build.gradle
buildscript {
    repositories {
        google()
        mavenCentral()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.1'  // 使用较新版本
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}